import React from "react";
import { SpinnerContainer, SpinnerOverlay } from "./style";

const Spinner = () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};

export default Spinner;
